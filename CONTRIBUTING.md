# 协作约定

本约定用于 3 人协作的课设项目。需求范围见 [PRD](docs/PRD.md)，任务和推进阶段见[可认领 Issue 清单](docs/ISSUE_BACKLOG.md)，提交材料见[课程要求](docs/COURSE_REQUIREMENTS.md)。

## Issue 认领

- 按任务认领，不预先固定成员负责哪个技术层。每个 Issue 一名主责，可邀请协作者；PR 至少由另一名成员评审。
- 认领前确认依赖、范围和验收条件；在 Issue 留下认领说明并设置 assignee。Issue 发布前，先在本地清单的认领表记录姓名和状态。
- 每人同时推进一个主要 Issue，避免一次占用整批任务。任务被阻塞时记录原因、依赖和交接材料，再协商认领其他就绪任务。
- 已被认领的任务先协调再共同开发；转交时说明已完成内容、分支、验证结果及剩余工作，不静默更换负责人。
- 初始清单使用 `MF-xx` 本地任务 ID；发布后登记实际 Issue 链接，不能把它当成远端 `#xx`。远端发布后由 Issue 维护 assignee 和状态，本地表只保留索引及依赖。
- 新任务或需要拆分的任务使用下方 [Issue 模板](#issue-模板)。较大的任务按可独立验收的结果拆子 Issue；父项只有在全部子项完成后才能关闭。

## 一项工作如何完成

1. 建立并认领 Issue，写明需求编号、目标、修改范围、依赖和验收条件；公共接口或业务规则先更新设计。
2. 从最新 `main` 建短期分支，例如 `feat/mf-06-auth`、`docs/mf-02-prototype`。一个分支聚焦一项可评审工作；应用骨架由 MF-04 统一建立，其他任务沿用共同配置。
3. 提交使用清晰的 Conventional Commits，例如 `docs: define creation workflow`。只提交相关文件，不混入他人未完成更改。
4. PR 描述问题、最终行为、关联 Issue 及 FR/T 编号、验证证据与未完成项。至少由另一名成员评审；涉及公共接口、任务/计费/权限时邀请相关 Issue 的认领者协同检查，不设永久模块负责人。
5. 通过必要检查后合并；验收条件满足、依赖完成且证据链接齐备后，才关闭 Issue。仅有 Mock、代码编写完成或 PR 创建均不自动等于验收完成。托管平台为 GitHub；本次仅建立文档基线，不把这些评审约定描述为已配置的分支保护规则。

## 文档与接口

- 需求、API、数据、状态、计费和验收口径必须一致；改变一个规则时同步修改受影响文档。
- 已确认事实、提案和待确认问题分别标明；将设计计划变成实现事实时补代码位置与测试证据。
- 先修改共享运行时 schema，再同步客户端和服务端；仅有 TypeScript 类型不能保证外部输入正确。
- 技术选择及理由统一更新 [ARCHITECTURE](docs/ARCHITECTURE.md)，任务和计费规则更新 [WORKFLOWS](docs/WORKFLOWS.md)；评审和变更历史留在 Issue/PR，不要求额外撰写 ADR 或 RFC。
- 图源保存在相应 Markdown 中；正式报告的图件必须与最新图源一致并经过渲染检查。

涉及公共接口的认领者先对齐输入输出、错误、权限及契约样例，再并行实现；任务与计费的状态和事务规则共同评审。Provider 认领者提供实际能力和错误样例，不让其他模块依赖未验证行为。修改共享 schema、迁移或配置时在 PR 中标明受影响模块，同步调整引用方或约定兼容期；每个阶段集成一次完整场景。

## 代码与验证

- 前端、API、Worker 和共享业务包采用 TypeScript。初始化时启用严格类型检查；不能以 `any` 或忽略错误掩盖接口不一致。
- 外部服务调用经过适配器；测试不得默认调用收费接口。真实 Provider 测试单独运行，记录服务、规格、成本和来源。
- 重点验证余额并发、重复调用、恢复、隔离与删除；不以没有行为意义的断言凑数量。
- 新功能有相应错误/空态和可观察结果；业务逻辑注释解释规则与边界，不逐句翻译代码。
- 仓库尚未建立工具链，没有可运行的类型、构建或测试命令；骨架建立后补入 README，并实际验证。

## 数据与贡献记录

不提交 API Key、`.env` 实值、会话令牌、个人声音样本、私人生成物、数据库文件或运行日志。种子数据与演示素材注明来源及允许用途，Mock 明确标识。

每名成员为自己认领的 Issue 维护设计、代码、测试和报告片段，通过 Issue → PR → 验收证据记录实际贡献。使用 AI 辅助时按课程或教师要求记录辅助范围；团队负责理解、修改和核验，不能把未运行的内容写成测试结论。引用资料与开源依赖保留来源，不复制他人项目冒充团队成果。

## Issue 模板

将下方正文复制到托管平台。`MF-xx` 是本地 ID，发布后用真实链接关联前置 Issue 与 PR。实现跨度过大时拆分子任务并更新依赖，不删除验收项来缩小范围。

```markdown
## 目标与范围

完成后用户或协作者能做什么，本项与其他任务的边界是什么。

## 关联与依赖

- 本地 ID / 阶段：MF-xx / P0、P1 或交付
- 需求与设计：FR-xx、NFR-xx、相关文档
- 前置 Issue：
- 可先使用的契约或 Mock：
- 需要同步的接口、迁移或共享配置：

## 验收条件

- [ ] 可观察的正常结果。
- [ ] 相关失败、权限和边界场景符合设计。
- [ ] 已附文档与实际验证证据，区分 Mock 和真实服务。

## 认领与完成证据

- 主责 / 评审人：未认领 / 待邀请另一名成员
- 状态：待依赖 / 可认领 / 进行中 / 待评审 / 阻塞 / 已完成
- 阻塞原因与交接记录：
- PR / commit、T-xx 验收记录：
- 运行环境、Provider、sourceMode（如涉及）：
- 截图或结果、仍未完成的内容：
```

## PR 描述参考

```text
目的与关联需求：
变更后的行为：
接口 / 数据 / 文档变化：
验证方式与证据：
限制、未实现项或需要协调的事项：
评审人：
```
